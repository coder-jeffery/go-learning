services:
  mysql:
    image: mysql:8.4
    container_name: coffee-inventory-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: coffee_db
      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
